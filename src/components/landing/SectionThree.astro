---
let softwareList = [
    {
        name: "Sistema de \n Punto de ventas",
    },
    {
        name: "Sistema de E-commerce",
    },
    {
        name: "Sistema de control de asistencia",
    },
    {
        name: "Sistema de Trámite documentario",
    },
    {
        name: 'Sistemas personalizados a medida'
    }
]
---
<section class="bg-gray-100 min-h-fit pt-5 md:pt-10 px-5 sm:px-0 pb-10 md:pb-16">
    <div class="container mx-auto grid gap-6 grid-cols-[repeat(auto-fit,minmax(150px,1fr))]">
     {softwareList.map((item, index) => (
        <div class:list={["grid grid-cols-1 gap-6 py-10 px-12 rounded-sm bg-white place-items-center text-center font-semibold", index % 2 === 0 ? 'text-orange-500' : 'text-blue-800' ]}>
            <span class="w-[151px]">{item.name}</span>
        </div>
     ))}
    </div>
</section>
